
var MaxJs = (function initMaxJs(glob) {

  //stores the patcher

  var mom = require('./src/mom');
  var cache = require('./src/global-cache');
  function maxJs(thisArg, opts){
    var defaults = {

    }
    var extended = reduce(parseOpts, thisArg);

  }
  return maxJs;
}(this))